{
  "hash": "52bf4ffe8ab897954fadb7b5455ccecf",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Python Reference\"\n---\n\nThis reference collects Python patterns used throughout the labs.  The goal is to keep code readable and mathematically connected.\n\n## Core libraries\n\n::: {#df9c470f .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\n```\n:::\n\n\n## Count vectors\n\n::: {#a953f225 .cell execution_count=2}\n``` {.python .cell-code}\nvocab = [\"data\", \"language\", \"model\", \"statistics\"]\ndoc = [\"language\", \"model\", \"model\", \"data\"]\ncounts = np.array([doc.count(token) for token in vocab])\ncounts\n```\n\n::: {.cell-output .cell-output-display execution_count=2}\n```\narray([1, 1, 2, 0])\n```\n:::\n:::\n\n\n## Cosine similarity\n\n::: {#445b7674 .cell execution_count=3}\n``` {.python .cell-code}\ndef cosine_similarity(x, y):\n    x = np.asarray(x, dtype=float)\n    y = np.asarray(y, dtype=float)\n    denom = np.linalg.norm(x) * np.linalg.norm(y)\n    if denom == 0:\n        return 0.0\n    return float(np.dot(x, y) / denom)\n```\n:::\n\n\n## Softmax\n\n::: {#956b9fd2 .cell execution_count=4}\n``` {.python .cell-code}\ndef softmax(z):\n    z = np.asarray(z, dtype=float)\n    z = z - np.max(z)\n    exp_z = np.exp(z)\n    return exp_z / exp_z.sum()\n```\n:::\n\n\n## Scaled dot-product attention\n\n::: {#9524e33c .cell execution_count=5}\n``` {.python .cell-code}\ndef attention(Q, K, V, mask=None):\n    Q = np.asarray(Q, dtype=float)\n    K = np.asarray(K, dtype=float)\n    V = np.asarray(V, dtype=float)\n    d_k = Q.shape[-1]\n    scores = Q @ K.T / np.sqrt(d_k)\n    if mask is not None:\n        scores = scores + mask\n    weights = np.apply_along_axis(softmax, 1, scores)\n    return weights @ V, weights\n```\n:::\n\n\n",
    "supporting": [
      "python-reference_files"
    ],
    "filters": [],
    "includes": {}
  }
}